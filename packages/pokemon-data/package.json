{
  "name": "@pokelab/pokemon-data",
  "private": true,
  "version": "0.0.0",
  "description": "PokeLab Pokémon reference data: full ingestion pipeline (fetch/cache -> normalize -> validate -> persist) and normalized types. Not a runtime PokéAPI client.",
  "license": "SEE LICENSE IN LICENSE",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "ingest": "tsx scripts/ingest.ts",
    "audit": "tsx scripts/audit.ts",
    "diagnose-form-identity": "tsx scripts/diagnose-form-identity.ts",
    "clean": "rm -rf dist .cache"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.47.10",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@pokelab/config": "workspace:*",
    "@types/pg": "^8.11.10",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
