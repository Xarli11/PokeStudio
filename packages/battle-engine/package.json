{
  "name": "@pokestudio/battle-engine",
  "private": true,
  "version": "0.0.0",
  "description": "PokeStudio battle-domain boundary around the Pokémon Showdown simulator (ADR-0003). UI and application code must not import pokemon-showdown directly.",
  "license": "SEE LICENSE IN LICENSE",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "pokemon-showdown": "0.11.11"
  },
  "devDependencies": {
    "@pokestudio/config": "workspace:*",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
